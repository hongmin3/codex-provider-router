# Progress

- 2026-09-13: Codex 0.154.0, ChatGPT login, DeepSeek Responses API 기준 Router 구축·설치.
- 기본 fallback은 `deepseek-flash + high`; `flash`, `pro`, `vision` alias와 reasoning 영구 설정 지원.
- OpenAI/DeepSeek 실 API, state simulation, checkpoint, Keychain, shell/apply_patch/Git/MCP 보존 테스트 완료.
- 제한: 동일 process hot-swap 대신 persisted-thread resume; Codex connect timeout/cost hard-stop은 정식 hook 부재로 미지원.
- 2026-09-13: DeepSeek 실패를 billing, quota, auth, network, server, unknown으로 분류하고 터미널에 한국어 원인·해결 방법을 표시하도록 확장.
- 2026-09-13: 프로젝트 루트에 `USAGE_AND_SPEC.md` 사용법·사양서 추가.
- 2026-09-13: Router child PTY에 실제 터미널 크기를 복사하고 `SIGWINCH`로 resize를 동기화하여 Codex TUI 줄바꿈 깨짐 수정.
- 2026-09-13: DeepSeek 공식 Codex catalog를 24시간마다 자동 확인하고 새 모델 발견 시 백업·검증·갱신·알림하는 기능과 `models list|check|refresh` 명령 추가.
- 2026-09-13: Codex의 `usage limit resets available`·잔여량 경고를 실제 한도 소진으로 오인해 DeepSeek `resume --last`로 전환하던 문제 수정. 명확한 hit/reached/exceeded/429만 감지.
- 2026-09-14: 기존 failover 엔진을 재사용하는 `ai` Model Router 추가. Local heuristic, Provider 후보 필터, 승인 UI, runtime model/reasoning override, dry-run/status/doctor, 익명 usage log, 승인형 provider failover 및 capability escalation을 구현.
- 기본 classifier는 OFF이며 일반 라우팅 추가 token은 0. DeepSeek status refresh는 공식 balance/models endpoint만 사용하고 OpenAI quota 확인용 LLM probe는 실행하지 않음.
- 2026-09-19: DeepSeek 전용 실행 명령 추가. `deep codex --yolo`(= `deep --yolo`)와 `codex-router deepseek --yolo`가 OpenAI 상태와 무관하게 DeepSeek profile로 Codex를 시작하며, 비대화형 `deep exec "..."`도 DeepSeek를 유지한다. Key가 없으면 Codex를 시작하지 않고 exit 78. 설치본 반영, 45개 테스트·`zsh -n`·실 DeepSeek 호출 확인.
- 2026-09-19: 기본 provider는 OpenAI primary + usage limit 시 DeepSeek fallback 그대로 유지. `codex --yolo` 자체를 DeepSeek 기본으로 바꾸는 것은 기존 Knowledge(purpose)와 충돌하므로 소유자 결정 대기.
- 2026-09-19: 소유자 확인 결과 `deep` 접두어 = DeepSeek, 일반 `codex` = GPT로 역할 분리 확정. macOS에도 `~/.local/bin/deep` 심볼릭 링크와 `codex-router deep|deepseek [CODEX_ARGS]`를 추가해 Windows `deep.cmd`와 동작·이름을 맞춤(첫 인자 `codex`는 양쪽 모두 제거). DeepSeek 실행 시 표시되던 `OpenAI cooldown` 문구를 실제 원인(`requested`/`OpenAI usage limit`)으로 수정. TTY 경로에서 `deep codex --help` = DeepSeek, `codex --help` = OpenAI 출력 확인.
- 2026-09-19: usage limit 감지 시 자동 DeepSeek 전환을 제거하고 `y/N` 확인 후에만 전환하도록 변경(기본값 전환 안 함). `codex` 세션 시작 시 cooldown으로 자동 DeepSeek 진입하는 경로에도 확인을 추가하고, 사용자가 거절하고 OpenAI가 성공하면 상태를 `OPENAI_ACTIVE`로 복구한다. 잔여량(83%/90%)이 충분한데도 오탐으로 전환된 사례를 계기로 확인 게이트를 추가함.
- 2026-09-19: 이어서 남아 있던 변경 검증 완료. 양수 잔여율의 `0% left` 부분 일치 방지, 손상된 state/catalog 처리, 비대화형 강제 DeepSeek 실행, 설치 전 catalog 검증 및 기존 설정 보존 회귀 테스트를 포함해 61개 테스트 통과. `zsh -n scripts/*.sh` 및 `git diff --check` 통과. 이번 검증에서는 설치본을 갱신하지 않음.
- 2026-09-19: 설치본이 갱신되지 않아 남아 있던 `/status` 오탐·종료 수정. Codex `/status` 화면의 `Weekly limit: 70% left`가 옛 `0%\s+left` 패턴에 부분 일치해 Codex를 종료시키고 전환 질문을 띄우던 문제를, lookbehind 수정이 포함된 저장소 src를 `~/.codex/router/codex_router.py`에 반영(백업 `~/.codex-backup/20260919-123014-18363`)하여 해결. `/status` 실제 문구 회귀 테스트 추가, 실제 wrapper로 `/status`를 열어 프롬프트 미발생 확인.
- 2026-09-19: DeepSeek 잔액 조회·경고 기능 추가. `codex-router balance`(`ai balance`)가 공식 `/user/balance`에서 USD 총액·충전·증정 잔액을 조회하고, `--json`은 자동화용 출력(`low_balance`, `threshold_usd` 포함), `balance threshold [금액]`은 경고 기준(기본 USD 1.00)을 읽고 저장한다. 조회 결과는 `provider-status.json`에 기록되어 `codex-router status`·`ai status`가 network 없이 같은 금액을 보여주고, 기준 미만이면 DeepSeek 세션 시작(수동 실행·usage limit 전환 모두)에서 경고를 표시한다. 잔액 부족은 provider를 중단시키지 않으며 `is_available: false`일 때만 `BALANCE_EXHAUSTED`로 기록한다. 111개 테스트·`zsh -n scripts/*.sh` 통과, 실제 API 조회 확인(USD 2.79). 소유자 승인 후 설치본 반영(백업 `~/.codex-backup/20260919-143215-75136`)하고 경고 기준을 일시적으로 5로 올려 실제 경고 출력까지 확인한 뒤 1로 되돌림. `write_config`가 config를 다시 쓸 때 template 주석을 잃던 기존 동작도 함께 고쳐 회귀 테스트를 추가함.
- 2026-09-19: DeepSeek 세션 진입·종료 시 잔액 표시 추가. 시작 배너에 `| Balance: USD x.xx`, 세션 종료 후 새로 조회한 `DeepSeek 잔액: USD x.xx`. 검증 중 회귀 2건을 잡아 고침. (1) 시작 시 잔액을 이 process에서 조회하면 macOS resolver thread 때문에 `run_pty`의 `forkpty()`가 multi-thread fork가 되어 Python DeprecationWarning이 뜨고 deadlock 위험이 생김 — 갱신을 자식 process(`codex_router.py balance --json`)로 옮기고, fork 전에 조회가 일어나지 않는지 검사하는 회귀 테스트 추가. (2) 새 테스트가 `mark_openai_active` 경로에서 실제 `~/.codex/router/provider-status.json`에 fixture 값을 기록 — session helper에서 `save_provider_status`를 mock하고, 두 테스트 모듈에 live state 스냅샷 guard(`tearDownModule`)를 추가해 어떤 테스트든 실제 상태를 건드리면 suite가 실패하도록 함(guard가 실제로 실패하는지 확인함). `ai status --refresh`가 `balance_checked_at`을 남기지 않아 매 세션 갱신이 돌던 것도 수정. 122개 테스트·`zsh -n`·설치본 PTY 실행(`script`로 실제 터미널) 확인, Codex 전역 지침 `~/.codex/AGENTS.md`도 실제 `codex exec` 실행으로 로드 확인.
